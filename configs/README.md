# Configurations

Configuration contracts will be added only after the data, evaluation, and
upstream implementation choices are separately frozen. Paths must be supplied
through environment variables or configuration, never hard-coded for one host.
